#!/usr/bin/env bash
#MISE description="Sync selected global coding-agent skills"
set -euo pipefail

repo_root="$(cd "$(dirname "${BASH_SOURCE[0]}")/../../.." && pwd)"
exec "$repo_root/scripts/agents/sync.ts" "$@"
