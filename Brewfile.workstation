# Portable human-workstation Homebrew layer.
# Run through brew-bundle.ts so the shared Brewfile installs first.

# Human desktop
cask "ghostty"
cask "1password"
cask "1password-cli"
cask "slack"
cask "chatgpt"
cask "claude"
cask "cursor"
cask "t3-code"
cask "zed"

# Platform development
cask "android-studio"

# Hardware security
brew "ykman"
