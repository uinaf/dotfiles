allowBuilds:
  msgpackr-extract: false
# Effect RC releases are excluded by name so version bumps touch only
# package.json. Bare names exempt every version of these packages.
minimumReleaseAgeExclude:
  - '@effect/platform-node-shared'
  - '@effect/platform-node'
  - effect
